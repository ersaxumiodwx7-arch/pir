// DEPRECATED — this was the entry point of an abandoned Vite rewrite.
// The canonical entry point is `src/index.js` (used by react-scripts via
// public/index.html). This file only exists to redirect anything that
// references it to the real app, so it can never load broken code.
import './index.js';
