// DEPRECATED — part of an abandoned Vite rewrite. The canonical root
// component is `src/App.js`; this file is kept only so any import of
// `App.jsx` resolves to the real app instead of broken code.
export { default } from './App.js';
