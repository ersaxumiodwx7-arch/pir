// DEPRECATED — part of an abandoned Vite rewrite. The canonical page is
// `src/pages/Responses.js`; this file is kept only so any import of
// `Responses.jsx` resolves to the real implementation instead of broken code.
export { default } from './Responses.js';
