// DEPRECATED — part of an abandoned Vite rewrite. The canonical page is
// `src/pages/FormBuilder.js`; this file is kept only so any import of
// `FormBuilder.jsx` resolves to the real implementation instead of broken code.
export { default } from './FormBuilder.js';
