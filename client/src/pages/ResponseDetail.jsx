// DEPRECATED — part of an abandoned Vite rewrite. The canonical page is
// `src/pages/ResponseDetail.js`; this file is kept only so any import of
// `ResponseDetail.jsx` resolves to the real implementation instead of broken code.
export { default } from './ResponseDetail.js';
