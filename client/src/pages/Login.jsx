// DEPRECATED — part of an abandoned Vite rewrite. The canonical page is
// `src/pages/Login.js`; this file is kept only so any import of `Login.jsx`
// resolves to the real implementation instead of broken code.
export { default } from './Login.js';
