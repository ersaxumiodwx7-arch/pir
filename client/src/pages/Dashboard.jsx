// DEPRECATED — part of an abandoned Vite rewrite. The canonical page is
// `src/pages/Dashboard.js`; this file is kept only so any import of
// `Dashboard.jsx` resolves to the real implementation instead of broken code.
export { default } from './Dashboard.js';
