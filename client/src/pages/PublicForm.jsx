// DEPRECATED — part of an abandoned Vite rewrite. The canonical public form
// page is `src/pages/FormViewer.js`; this file is kept only so any import of
// `PublicForm.jsx` resolves to the real implementation instead of broken code.
export { default } from './FormViewer.js';
