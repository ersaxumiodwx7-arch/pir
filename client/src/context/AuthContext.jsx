// DEPRECATED — part of an abandoned Vite rewrite. The canonical auth context
// is `src/context/AuthContext.js`; this file is kept only so any import of
// `AuthContext.jsx` resolves to the real implementation instead of broken code.
export { AuthProvider, useAuth } from './AuthContext.js';
